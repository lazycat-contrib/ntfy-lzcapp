params:
  - id: tz
    type: string
    name: "Timezone"
    description: "Server timezone (e.g. Asia/Shanghai, UTC)"
    default_value: "UTC"
    optional: true

locales:
  zh:
    tz:
      name: "时区"
      description: "服务器时区（如 Asia/Shanghai、UTC）"
